import { rem } from "../font";

const light = {
    $rem: rem,
    $primary: 'blue',
    $accent: 'green',
    $background: 'white',
    $surface: 'white',
    $disabled: 'grey',
    $danger: 'red',
    $text: 'black'
}

export default light;